<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <script src="https://cdn.tailwindcss.com"></script>
    <title>Document</title>
  </head>
  <body>
    <div
      class="w-full min-h-screen bg-zinc-900 flex items-center justify-center"
    >
      <form
        action="/update/<%=post._id%>"
        method="POST"
        class="bg-zinc-800 p-8 rounded-2xl shadow-lg w-full max-w-md space-y-4"
      >
        <h1 class="text-white text-3xl font-semibold text-center">
          Edit Post
        </h1>

        <textarea
          type="text"
          placeholder="Whats on your mind"
          name="content"
          class="w-full py-1 rounded-lg bg-zinc-700 text-white placeholder-gray-400 focus:outline-none focus:ring-2 focus:ring-blue-500"
        >
        <%=post.content%>
    </textarea>

        

        <input
          type="submit"
          value="Edit"
          class="w-full py-2 bg-blue-600 text-white rounded-lg hover:bg-blue-700 transition"
        />
      </form>
    </div>
  </body>
</html>
